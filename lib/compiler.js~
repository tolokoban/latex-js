"use strict";

var Parser = require("./parser");

module.exports = function(content, dirname, output) {
    var parsing = Parser( content );
    var index = 0;
    function next() {
        if( index >= parsing.length ) {
            // It's over.
            return;
        }
        var item = 
    }
};
